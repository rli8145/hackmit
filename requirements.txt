# Company Brain core runs on the Python 3.10+ STANDARD LIBRARY ALONE.
#   python run_demo.py
#   python -m company_brain.api
#
# The packages below are only needed to activate the real integrations
# (each behind an env key; see .env.example). Install what you use:

openai>=1.0.0            # OPENAI_API_KEY   — LLM extraction
deepgram-sdk>=3.0.0      # DEEPGRAM_API_KEY — audio → text ingestion
elevenlabs>=1.0.0        # ELEVENLABS_API_KEY — spoken answers
elasticsearch>=8.0.0     # ELASTIC_URL       — production search backend
python-dotenv>=1.0.0     # load .env automatically (optional convenience)
